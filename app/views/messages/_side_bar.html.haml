.side
  .side__header
    .side__header__box
      .side__header__box--name
        = current_user.name
      .side__header__box__icons
        = link_to new_group_path do
          = icon("fa", "edit", class: "side__header__box__icons--edit")
        = link_to edit_user_path(current_user) do
          = icon("fa", "cog", class: "side__header__box__icons--cog")
  .side__groupList
    - current_user.groups.each do |group|
      = link_to "#", class: "side__groupList--name" do
        = group.name
      .side__groupList--message
        まだメッセージがありません